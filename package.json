{
  "name": "stripe-webhook-receiver",
  "version": "0.1.0",
  "bin": {
    "stripe-webhook-receiver": "bin/stripe-webhook-receiver.js"
  },
  "scripts": {
    "build": "cd lambdas/stripe-verifier && npm install && cd ../../ && tsc",
    "watch": "cd lambdas/stripe-verifier && npm install && cd ../../ && tsc -w",
    "test": "jest",
    "deploy": "npm run build && npm test && cdk deploy"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.80.0",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "aws-cdk": "1.80.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "@aws-cdk/aws-apigatewayv2": "^1.80.0",
    "@aws-cdk/aws-apigatewayv2-integrations": "^1.80.0",
    "@aws-cdk/aws-lambda": "^1.80.0",
    "@aws-cdk/aws-logs": "^1.80.0",
    "@aws-cdk/aws-secretsmanager": "^1.80.0",
    "@aws-cdk/core": "1.80.0",
    "@simple-cdk-constructs/queue-with-dlq": "^1.0.1",
    "source-map-support": "^0.5.16"
  }
}
